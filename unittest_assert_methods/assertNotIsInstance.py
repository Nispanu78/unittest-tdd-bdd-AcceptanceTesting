import unittest

# assertNotIsInstance(obj, class, msg=None [to check that an object is NOT an instance of a specified class])
def test_assert_is_not_instance(self):
    self.asserNotIsInstance(1, str)

if __name__ == '__main__':
    unittest.main()
