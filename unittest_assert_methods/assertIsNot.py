import unittest

# assertIsNot(expr1, expr2, msg=None [to chech that expr1 and expr2 are not the same object])
def test_assert_is_not(self):
    self.assertIsNot([], [])

if __name__ == '__main__':
    unittest.main()
